{% extends 'user/index.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-sm-9 user_data">
                <div class="row">
                    <div class="user_photo">
                        {% if current_user.image is null %}
                            <img src="/img/default.png" alt="">
                        {% else %}
                            <img src="{{ current_user.image }}" alt="">
                        {% endif %}
                    </div>
                    <div class="user_info">
                            {{ form_start(change_image_form, {
                                'attr' : {
                                    'enctype' : 'multipart/form-data'
                                }
                            }) }}
                            <div class="form-group">
                                <label for="username">Select image</label>
                                {{ form_widget(change_image_form.image) }}
                            </div>
                            <div class="form-group">
                                <button type="submit" class="btn btn-primary">Change</button>
                            </div>
                            {{ form_end(change_image_form) }}
                        </form>
                    </div>
                </div>
            </div>
            {% block user_menu %}
                {{ parent() }}
            {% endblock %}
        </div>
    </div>
{% endblock %}